<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        /* Custom style for score display */
        .score-box {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 5px;
            font-weight: bold;
            min-width: 70px;
            text-align: center;
        }
        .score-passed {
            background-color: #d4edda; /* Light green */
            color: #155724; /* Dark green */
            border: 1px solid #c3e6cb;
        }
        .score-failed {
            background-color: #f8d7da; /* Light red */
            color: #721c24; /* Dark red */
            border: 1px solid #f5c6cb;
        }
        .score-none {
            background-color: #fefefe; /* White */
            color: #6c757d; /* Gray */
            border: 1px solid #ccc;
        }
        /* Custom CSS for right-alignment */
        .align-right {
            text-align: right;
        }

        /* Optional: Improves the look of the score badge slightly */
        .score-badge {
            display: inline-block;
            padding: 5px 8px;
            border-radius: 5px;
            font-weight: bold;
        }
        .score-passed { background-color: #d4edda; color: #155724; }
        .score-failed { background-color: #f8d7da; color: #721c24; }
    </style>
</head>
<body class="bg-light">
    <%- include('partials/header', { user: user }) %>

    <div class="container mt-4">
        <h1 class="mb-4 text-success">Student Dashboard <i class="bi bi-mortarboard-fill"></i></h1>

        <div class="row mb-4">
            <div class="col-md-12">
                <form class="d-flex" action="/student/dashboard" method="GET">
                    <input class="form-control me-2" type="search" placeholder="Search quizzes by Title, Subject, or Year..." 
                           aria-label="Search" name="search" value="<%= search %>">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-header bg-white">
                <h5 class="mb-0">Assigned Quizzes (<%= quizzes.length %>)</h5>
            </div>
            <div class="card-body p-0">
                <% if (quizzes.length === 0) { %>
                    <div class="alert alert-warning m-3 text-center">
                        No quizzes are currently available or none match your search criteria.
                    </div>
                <% } else { %>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0 align-middle">
                            <thead>
                                <tr>
                                    <th>Quiz Title</th>

                                    <th>Subject</th> 

                                    <th>Year</th>

                                    <th class="align-right">Your Progress (/20)</th> 

                                    <th class="align-right">Action</th> 
                                </tr>
                            </thead>
                                    <tbody>
                                        <% quizzes.forEach(quiz => { %>
                                            <tr>
                                                <td>
                                                    <strong><%= quiz.title %></strong>
                                                    <div style="color: #6c757d; font-size: 0.85em;"><%= quiz.description || 'No description available' %></div>
                                                </td>

                                                <td><%= quiz.subject %></td> 

                                                <td><%= quiz.year %></td> 

                                                <td class="align-right">
                                                    <% if (quiz.highest_score !== null) { %>
                                                        <% 
                                                            const scoreClass = quiz.highest_score >= 10 ? 'score-passed' : 'score-failed'; 
                                                            const scoreText = quiz.highest_score >= 10 ? 'Passed' : 'Failed'; 
                                                        %>
                                                        <span class="score-badge <%= scoreClass %>">
                                                            <%= quiz.highest_score.toFixed(1) %> / 20
                                                        </span>
                                                    <% } else { %>
                                                        <span style="color: #6c757d;">N/A</span>
                                                    <% } %>
                                                </td>

                                                <td class="align-right">
                                                    <% if (quiz.highest_score === null) { %>
                                                        <a href="/student/quizzes/<%= quiz.id %>/take" style="padding: 6px 12px; background-color: #007bff; color: white; text-decoration: none; border: none; border-radius: 4px;">
                                                            Start Quiz
                                                        </a>
                                                    <% } else { %>
                                                        <a href="/student/quizzes/<%= quiz.id %>/history" style="padding: 6px 12px; background-color: #17a2b8; color: white; text-decoration: none; border: none; border-radius: 4px; margin-right: 5px;">
                                                            History
                                                        </a>
                                                        <a href="/student/quizzes/<%= quiz.id %>/take" style="padding: 6px 12px; background-color: #6c757d; color: white; text-decoration: none; border: none; border-radius: 4px;">
                                                            Retake
                                                        </a>
                                                    <% } %>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    </tbody>
                        </table>
                    </div>
                <% } %>
            </div>
        </div>

        <div class="mt-4 p-3 bg-white rounded shadow-sm">
            <h5 class="text-secondary"><i class="bi bi-info-circle-fill"></i> Progress Rules</h5>
            <small class="text-muted">
                Your progress is based **only** on your latest completed submission per quiz. Starting a quiz without submitting it will not affect your progress.
            </small>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>